# Facility Raw Data

This directory is reserved for the canonical challenge facility export.

Stage the export with:

```bash
python3 -m src.ingestion.facility_intake /path/to/facilities.csv --stage
```

Do not manually normalize or overwrite the staged raw file.

